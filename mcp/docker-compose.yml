version: "3.9"

services:
  vlam-host:
    build: ./host
    ports:
      - "8000:8000"
    env_file:
      - ./host/.env
    volumes:
      - ./servers:/app/../servers:ro
    depends_on:
      - kvk
      - koop
      - regelrecht
      - rvo

  kvk:
    build: ./servers/kvk
    # MCP-servers communiceren via stdio, niet via netwerk
    # Ze worden als subprocess gestart door de host

  koop:
    build: ./servers/koop

  regelrecht:
    build: ./servers/regelrecht

  rvo:
    build: ./servers/rvo
